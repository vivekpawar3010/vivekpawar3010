import LeetCodeDashboard from "../../components/LeetCodeDashboard";

export default function AboutPage() {
  return (
    <main style={{ padding: "1rem 1.25rem 2rem", minHeight: "calc(100vh - 72px)" }}>
      <LeetCodeDashboard />
    </main>
  );
}

